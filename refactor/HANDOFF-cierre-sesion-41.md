# HANDOFF — cierre sesión 41

> Sesión 41 cerrada el 27 mayo 2026, ~11:30 hora local.
> Sesión 41 = **item 9 NUEVO §10.1 (polling 150ms zoom Y L2922 `_SessionInner.js`) cerrado estructuralmente "no aplica empíricamente" patrón item 8 s39 + reclasificación deuda Bloque 4 Fase 6 trading domain + §51 NUEVA s39 aplicada al carácter en arranque + fe-de-erratas HANDOFF s40 §5 (L2914→L2922)**.
> **Resultado al carácter sin maquillaje**: **PASO 0 baseline bicapa REAL ✓ (10 checks). §51 NUEVA s39 aplicada al item 9: re-verificación empírica bytes-on-disk confirma polling 150ms VIVO no fantasma. Caracterización arquitectónica empírica L2900-L2940 + typings.d.ts LWC oficial 5.x (IPriceScaleApi L2160-L2188) concluyó al carácter: LWC oficial NO expone canal pub/sub price scale change. Comentario L2918 "sin contrato formal" ratificado bytes-on-disk. Polling 150ms es solución pragmática justificada por la propia API LWC, NO residuo pre-LWC-oficial como 5f.2**. 3 estrategias evaluadas (A canal pub/sub LWC = descartada inexistente; B migración ISeriesPrimitive = ALTO coste fuera scope §10.1, reclasificada Bloque 4 Fase 6; C dejar polling justificado + cerrar "no aplica empíricamente" = ELEGIDA). **Item 9 NUEVO §10.1 CERRADO ESTRUCTURALMENTE AL CARÁCTER POST-CARACTERIZACIÓN EMPÍRICA EN REPO**: 1 commit `f70df33` docs(plan-maestro) + push origin/main fast-forward + 5 Edits docs-only PLAN-MAESTRO-POST-S40.md (234→237 líneas, +3 netas).
> **CERO código tocado al carácter en s41**. `components/_SessionInner.js` baseline post-s40 md5 `6eaa3b56a8252277e9073245623f8f45` **INTACTO**. Runtime Vercel `ae29f16` (cleanup 5f.2 s40) **INTACTO** — deploy automático docs-only sin cambio observable usuario final.
> **3 invariantes fase 4 intactas vigesimosegunda sesión consecutiva al carácter**: `cr.series.setData|update` = 0 en `_SessionInner.js`, `computePhantomsNeeded` = 3 en `_SessionInner.js`, Cluster A §1.7 (`lib/chartViewport.js` header §1.7 protegido) intocado.
> **2 errores §9.4 propios CTO registrados al carácter en s41 sin maquillaje**: error #1 instrucción ambigua PASO 2 — emití Edit 1 + verificación bicapa en mismo mensaje sin separar orden ejecución (paso aislado vs verificación post). Ramón ejecutó solo verificación → md5 idéntico baseline reveló Edit no aplicado. Cazado bicapa REAL discriminante. Beneficio colateral: baseline pre-Edit ratificado intacto al carácter. Error #2 propuesta heredoc literal pegado en chat para HANDOFF ~400 líneas viola patrón canónico bicapa entrega HANDOFFs heredado s23-s40 (entrega como archivo descargable + `mv ~/Downloads/` → `refactor/` + commit + push). HANDOFF s32 §7.1 documentaba patrón verbatim — leído en project_knowledge_search arranque s41, ignorado al redactar HANDOFF s41. Cazado por Ramón verbatim "pero k es todos estos mensajes? asi fragmentados, separados.... si es el hndoff damelo como archivo, lo descargo y me das los comandos pertinentes". Disciplina bicapa estricta cazó 2/2.
> **1 lección operativa nueva s41 al carácter**: artifact `</p>` renderer Claude Code UI ≠ bytes-on-disk reales. Verificación discriminante zsh nativo Ramón (`grep -c "</p>"` = 0) ratifica bytes propios, descarta contaminación render UI. Patrón análogo §51 NUEVA s39 reforzado: ratificación bicapa propia zsh nativo discriminante sobre output Claude Code chat UI.
> **1 lección §54 NUEVA al carácter**: HANDOFFs largos (>100 líneas) ENTREGABLES como archivo descargable (vía `create_file` en sandbox CTO web → Ramón descarga → `mv ~/Downloads/` a `refactor/`), NUNCA pegados verbatim en chat ni redactados vía heredoc Claude Code instruido desde CTO web. Patrón canónico bicapa s23-s40 documentado HANDOFF s32 §7.1. Origen: error #2 s41 sistémico.
> **1 fe-de-erratas HANDOFF s40 §5 al carácter**: HANDOFF s40 §5 declaró `Post-Edit s40 polling 150ms zoom Y se renumera a L2914 (consecuencia -13 Edit A)`. Bytes-on-disk real post-s40 completo = **L2922**. Aritmética correcta: 2927 - 13 (Edit A) + 8 (Edit B) = 2922. HANDOFF s40 §5 contó solo Edit A olvidando Edit B. Match clase §52 NUEVA — declarar línea exacta sin verificar mecánicamente bytes-on-disk post-completo. NO afecta runtime ni invariantes. Errata documental corregida en 3 sitios PLAN MAESTRO: §1.2 L47 + §2.1 L87 + §2.4 L122.
> **Lección §14 vigesimocuarta sesión consecutiva al carácter MULTI-INSTANCIA**: 4 instancias decisivas s41 catalogadas §9.
> Próxima sesión = sesión 42. Items §10.1 elegibles restantes detallados §10.

---

## §0 — Estado al cierre sesión 41, sin maquillaje

**Sesión 41 produjo 1 commit docs-only al carácter en local main + push origin/main + deploy Vercel automático (bundle idéntico, runtime efectivo intacto)**:

- `f70df33 docs(plan-maestro): cerrar item 9 §10.1 no aplica empiricamente + reclasificar deuda Bloque 4 Fase 6 trading domain` — 1 archivo modificado, 11 insertions, 8 deletions (+3 netas)

HEAD local main al cierre s41 = `f70df33` sobre `0106ad3` (PLAN-MAESTRO s40) sobre `efa197f` (HANDOFF s40) sobre `ae29f16` (cleanup 5f.2 s40 RUNTIME EFECTIVO) sobre `122a8d2` (HANDOFF s39).

`origin/main` post-cierre s41 = `f70df33` (push completado al carácter via Ramón directo zsh nativo).

Producción Vercel runtime efectivo `ae29f16` **SIN CAMBIO desde 26 may 2026 ~12:30 hora local s40**. Deploy automático `f70df33` re-construye bundle idéntico (docs-only, archivos `refactor/*.md` no incluidos en build Next.js).

**Realidad sin maquillaje al carácter**:

1. **PASO 0 baseline verificación bicapa REAL** ejecutado al carácter por Ramón en zsh con output verbatim transcrito (10 checks). Detalle §1.

2. **§51 NUEVA s39 aplicada al carácter sobre item 9 §10.1 ANTES de asumir vivo**: grep amplio `setInterval` retornó 2 matches discriminados al carácter (L2922 polling 150ms candidato VIVO + `useAuth.js:80` fix s32 legítimo). Polling 300ms L422 AUSENTE confirma cleanup 5f.2 s40 ratificado. Detalle §2.

3. **Caracterización arquitectónica empírica L2900-L2940 + typings.d.ts LWC oficial 5.x**: bloque polling 150ms + función `update` `priceToCoordinate` + comentario L2918 "sin contrato formal" + typings IPriceScaleApi L2160-L2188 (`applyOptions`/`options`/`setVisibleRange`/`getVisibleRange` solo) + canales pub/sub LWC oficial verificados (`subscribeCrosshairMove` L1639, `subscribeVisibleTimeRangeChange` L2888, `subscribeVisibleLogicalRangeChange` L2293, `subscribeSizeChange` L2934). **CERO canal pub/sub sobre price scale change / zoom Y**. Detalle §3.

4. **Veredicto al carácter sin maquillaje**: 3 estrategias evaluadas. Estrategia A descartada (canal inexistente). Estrategia B (ISeriesPrimitive migración) ALTO coste, fuera scope §10.1 → reclasificada deuda Bloque 4 Fase 6 trading domain. Estrategia C ELEGIDA (cerrar "no aplica empíricamente" + polling justificado). Detalle §4.

5. **5 Edits docs-only PLAN-MAESTRO-POST-S40.md al carácter** (234→237 líneas, +3 netas):
   - Edit 1: §1.2 fila item 9 (L47) — `⏳ ABIERTO prioridad 1 s41` → `✅ CERRADO "no aplica empíricamente"` + `L2914`→`L2922`
   - Edit 2: §2.1 fila s41 (L87) — caracterización empírica + reclasificación deuda
   - Edit 3: §2.4 Bloque 4 Fase 6 (L122) — nota PositionsOverlay/OrdersOverlay → ISeriesPrimitive (deuda heredada)
   - Edit 4: §5 criterio salida (L214) — `3 items abiertos: 3, 5, 9 NUEVO` → `2 items abiertos: 3, 5`
   - Edit 5: §6 próxima sesión (L225) — `sesión 41` → `sesión 42`
   Detalle §5.

6. **2 archivos NUEVOS/MODIFICADOS al carácter en s41**:
   - `refactor/PLAN-MAESTRO-POST-S40.md` MODIFICADO 234→237 líneas (+3 netas) md5 `410fcf9d764dc8b882a2c3a5c8842295` → `197c0f13dd58daec17d7edd448f95ca3`
   - `refactor/HANDOFF-cierre-sesion-41.md` NEW (este documento)

7. **CERO archivos de código modificados al carácter en s41**. `components/_SessionInner.js` baseline post-s40 md5 `6eaa3b56a8252277e9073245623f8f45` INTACTO.

8. **2 errores §9.4 propios CTO registrados al carácter** sin maquillaje. Detalle §6.

9. **1 lección operativa nueva al carácter** (artifact `</p>` renderer Claude Code UI). Detalle §7.

10. **1 lección §54 NUEVA al carácter** (HANDOFFs largos entregables como archivo descargable). Detalle §7.

11. **1 fe-de-erratas HANDOFF s40 §5 corregida al carácter** (L2914→L2922). Detalle §8.

12. **3 invariantes fase 4 intactas vigesimosegunda sesión consecutiva al carácter** (bicapa REAL ejecutada PASO 0 + verificación post-cleanup PASO 7):
    - `cr.series.setData|cr.series.update` solo aparecen en `lib/chartRender.js` (grep en `_SessionInner.js` retornó 0)
    - `computePhantomsNeeded` aparece exactamente 3 veces en `_SessionInner.js`
    - Cluster A `lib/chartViewport.js` §1.7 intocado — `head -5` retornó header §1.7 protegido verbatim

13. **Working tree clean al cierre s41 al carácter**:
    - `git status --short` → vacío (post-push)
    - `git rev-parse --short HEAD` → `f70df33` (pendiente añadir commit HANDOFF s41 al cerrar)
    - HEAD local = origin/main = `f70df33`
    - Runtime Vercel efectivo = `ae29f16` (sin cambio)

---

## §1 — PASO 0 baseline verificación bicapa REAL

Sub-paso 1a ejecutado por Ramón en zsh — output verbatim transcrito (§49):

```
$ git status --short
$ git rev-parse --short HEAD
0106ad3
$ git log --oneline -5 | cat
0106ad3 docs(plan-maestro): redactar PLAN-MAESTRO-POST-S40 - refactor completo + features bloqueantes hasta apertura alumnos
efa197f docs(handoff): cierre sesion 40 - cleanup 5f.2 polling cerrado estructuralmente en produccion + 7 errores §9.4 propios CTO + lecciones §52 NUEVA + §53 NUEVA + §51 NUEVA s39 aplicada al caracter
ae29f16 cleanup(5f.2): reemplazar polling 300ms getSelected() con suscripcion reactiva subscribeClick LWC oficial
122a8d2 docs(handoff): cierre sesion 39 - cleanup 5e.4 debugCtx cerrado estructuralmente en produccion + items 7/8 cerrados (fantasma + no aplica empiricamente) + leccion §51 NUEVA
e44bb9b cleanup(5e.4): eliminar parametro muerto debugCtx en applyNewBarUpdate
```

Sub-paso 1b — wc + md5:

```
$ wc -l lib/chartRender.js components/_SessionInner.js
     141 lib/chartRender.js
    3045 components/_SessionInner.js
    3186 total
$ md5 lib/chartRender.js components/_SessionInner.js
MD5 (lib/chartRender.js) = 5af39d6036c7852a86249b74188a024e
MD5 (components/_SessionInner.js) = 6eaa3b56a8252277e9073245623f8f45
```

Sub-paso 1c — 3 invariantes fase 4 verificación REAL:

```
$ grep -c "cr\.series\.setData\|cr\.series\.update" components/_SessionInner.js
0
$ grep -c "computePhantomsNeeded" components/_SessionInner.js
3
$ head -5 lib/chartViewport.js
/**
 * Viewport layer — fase 3 del refactor data-layer.
 *
 * Este módulo es el ÚNICO punto del proyecto que escribe al viewport del chart
 * (chart.timeScale().setVisibleLogicalRange y chart.timeScale().scrollToPosition).
```

Baseline al carácter ratificado bicapa (10 checks):

| Check | Esperado | Real | OK |
|---|---|---|---|
| `git status --short` | vacío | vacío | ✓ |
| HEAD | `0106ad3` | `0106ad3` | ✓ |
| log -5 | 0106ad3 + efa197f + ae29f16 + 122a8d2 + e44bb9b | íd. verbatim | ✓ |
| `wc -l chartRender.js` | 141 | 141 | ✓ |
| `wc -l _SessionInner.js` | 3045 | 3045 | ✓ |
| md5 `chartRender.js` | `5af39d60...` | exacto | ✓ |
| md5 `_SessionInner.js` | `6eaa3b56...` | exacto | ✓ |
| grep cr.series.setData\|update | 0 | 0 | ✓ |
| grep computePhantomsNeeded | 3 | 3 | ✓ |
| head -5 chartViewport.js | header §1.7 | exacto | ✓ |

3 invariantes fase 4 PASS al carácter **vigesimosegunda sesión consecutiva** (pre-cleanup s41). Cluster A `lib/chartViewport.js` §1.7 intocado. Runtime producción `ae29f16` = bytes-on-disk locales al carácter pre-s41.

PASO 0 CERRADO al carácter.

---

## §2 — §51 NUEVA s39 aplicada al item 9 §10.1 ANTES de asumir vivo

Grep amplio discriminante:

```
$ grep -rn "setInterval" components/ pages/ lib/ | head -20
components/_SessionInner.js:2922:    const iv=setInterval(update,150)
lib/useAuth.js:80:    const revalidateInterval = setInterval(revalidate, 45000)
```

Discriminación al carácter:

| Línea | Match | Caracterización |
|---|---|---|
| `_SessionInner.js:2922` | `setInterval(update,150)` | **ITEM 9 candidato VIVO** confirmado bytes-on-disk |
| `useAuth.js:80` | `setInterval(revalidate, 45000)` | 45s revalidación auth fix s32 LEGÍTIMO — NO tocar |

**Polling 300ms L422 AUSENTE** = ratifica cleanup 5f.2 cerrado s40 al carácter.

**Veredicto al carácter**: item 9 NUEVO §10.1 VIVO confirmado bytes-on-disk. NO fantasma documental (a diferencia item 7 §10.1 s39).

§51 NUEVA s39 satisfecha al carácter en arranque s41.

---

## §3 — Caracterización arquitectónica empírica L2900-L2940 + typings.d.ts LWC oficial 5.x

### §3.1 Bloque polling 150ms L2920-L2925 contexto verbatim

```
$ sed -n '2900,2940p' components/_SessionInner.js
[output verbatim transcrito - función update con priceToCoordinate sobre positions/pendingOrders, setLines(all), useCallback deps [positions,pendingOrders,chartMap,activePair,dataReady]]

  // Polling 150ms — defensa contra zoom Y u otros eventos sin contrato formal.
  useEffect(()=>{
    if(!dataReady) return
    update()
    const iv=setInterval(update,150)
    return()=>clearInterval(iv)
  },[update,dataReady])

  // Recálculo inmediato cuando bumpea chartTick (cambio TF / dataset — sub-fase 5d.6).
  useEffect(()=>{ update() },[chartTick,update])
```

Función `update` `useCallback` deps `[positions,pendingOrders,chartMap,activePair,dataReady]` calcula coordenadas Y via `cr.series.priceToCoordinate(price)` para positions abiertas (entry/SL/TP) + pendingOrders (entry/SL/TP) → setea `setLines(all)` que alimenta overlay React JSX líneas precio.

Polling 150ms L2920-L2925:
- deps `[update, dataReady]`
- cleanup `return()=>clearInterval(iv)` (intacto fuera scope 5f.2 al carácter)
- comentario L2918 declara explícitamente: **"defensa contra zoom Y u otros eventos sin contrato formal"**

useEffect L2929 chartTick complementario (TF change / dataset switch sub-fase 5d.6) ya canalizado pub/sub.

### §3.2 Canales pub/sub LWC oficial 5.x — typings.d.ts grep exhaustivo

```
$ grep -rn "subscribePriceScale\|subscribeSizeChange\|priceScaleChange\|onPriceScale\|priceScaleApplyOptions" node_modules/lightweight-charts/dist/typings.d.ts 2>/dev/null | head -20
node_modules/lightweight-charts/dist/typings.d.ts:2934: subscribeSizeChange(handler: SizeChangeEventHandler): void;
node_modules/lightweight-charts/dist/typings.d.ts:2940: unsubscribeSizeChange(handler: SizeChangeEventHandler): void;
```

```
$ grep -n "subscribeVisible\|subscribeCrosshair\|subscribeChartType\|priceRangeChange\|IPriceScaleApi\|VisibleRange\|PriceScale" node_modules/lightweight-charts/dist/typings.d.ts | head -30
L1639: subscribeCrosshairMove(handler: MouseEventHandler<HorzScaleItem>): void;
L1649: unsubscribeCrosshairMove(handler: MouseEventHandler<HorzScaleItem>): void;
L2160: export interface IPriceScaleApi {
L2166:  applyOptions(options: DeepPartial<PriceScaleOptions>): void;
L2172:  options(): Readonly<PriceScaleOptions>;
L2182:  setVisibleRange(range: IRange<number>): void;
L2188:  getVisibleRange(): IRange<number> | null;
L2293: chart.timeScale().subscribeVisibleLogicalRangeChange(onVisibleLogicalRangeChanged);
L2888: subscribeVisibleTimeRangeChange(handler: TimeRangeChangeEventHandler<HorzScaleItem>): void;
L2934: subscribeSizeChange(handler: SizeChangeEventHandler): void;
```

API canales pub/sub LWC oficial 5.x al carácter:

| Canal | Línea typings | Cubre |
|---|---|---|
| `subscribeCrosshairMove` | L1639 | Mouse move cursor — NO zoom Y |
| `subscribeVisibleTimeRangeChange` | L2888 | Zoom/pan **horizontal** (timeScale) |
| `subscribeVisibleLogicalRangeChange` | L2293 ref | Zoom/pan **horizontal** logical |
| `subscribeSizeChange` | L2934 | Resize container chart |

**IPriceScaleApi L2160-L2188** expone solo `applyOptions`/`options`/`setVisibleRange`/`getVisibleRange` (mutación + query síncrona). **CERO `subscribe*` sobre price scale change**.

### §3.3 Veredicto caracterización empírica al carácter

**LWC oficial 5.x NO expone canal pub/sub price scale change / zoom Y**. Comentario L2918 `"sin contrato formal"` es **diagnóstico arquitectónico correcto bytes-on-disk** ratificado empíricamente. Los desarrolladores previos caracterizaron correctamente y eligieron polling defensivo consciente.

Item 9 NUEVO **NO es análogo simétrico a 5f.2**:
- En 5f.2 existía canal LWC oficial vivo (`subscribeClick`) → polling era residuo pre-LWC-oficial
- En item 9 NO existe canal pub/sub price scale change → polling 150ms es solución pragmática justificada por la propia API LWC

---

## §4 — Veredicto cierre item 9 + 3 estrategias evaluadas

3 estrategias evaluadas al carácter:

| # | Estrategia | Viable | Coste | Veredicto |
|---|---|---|---|---|
| A | Migrar a canal pub/sub LWC oficial análogo a 5f.2 | **NO** — canal inexistente | — | Descartada bytes-on-disk |
| B | Migrar a `ISeriesPrimitive` (patrón fase 5g KillzonesPrimitive) — pipeline interno LWC invoca `updateAllViews` en TODOS los paths incluido zoom Y | Sí | ALTO — refactor mayor PositionsOverlay/OrdersOverlay (~200-400 líneas, sesión dedicada) | Fase arquitectónica futura — fuera scope cleanup §10.1 |
| C | Dejar polling 150ms justificado bytes-on-disk + cerrar item 9 "no aplica empíricamente" (patrón item 8 s39) | Sí | 0 | **ELEGIDA al carácter** |

**Patrón cierre análogo a item 8 s39 §10.1** (CustomDrawingsOverlay.js): caracterización empírica concluyó "no aplica empíricamente" — el polling NO es residuo pre-LWC-oficial, es solución pragmática justificada por API LWC. Refactor estructural requiere `ISeriesPrimitive` migración (Bloque 4 Fase 6 trading domain según PLAN MAESTRO §2.4, sesiones s59-s73), NO cleanup §10.1.

**Justificación elegir Estrategia C al carácter sin maquillaje**:
1. **LWC oficial 5.x NO expone canal pub/sub price scale change** — caracterización empírica typings IPriceScaleApi L2160-L2188 solo expone mutación/query síncrona.
2. **§15 obliga NO improvisar fix sin diagnóstico** — sin canal pub/sub no hay cleanup posible scope §10.1.
3. **Migración `ISeriesPrimitive` (estrategia B)** encaja **Bloque 4 Fase 6 trading domain** (PLAN MAESTRO §2.4). CLAUDE.md §1 + principio "debt belongs in its phase" violado si se hace ahora.
4. **Precedente bytes-on-disk al carácter**: item 8 s39 §10.1 cerrado como "no aplica empíricamente" — veredicto válido §10.1 ratificado.
5. **§48 LWC oficial precede vendor fork** ratificado — primitive nativo será el camino correcto cuando llegue Bloque 4.

Decisión Ramón al carácter via §14 input encriptado "lo que sea lo mejor para el proyecto" + "lo k sea lo mejor" → delegación juicio CTO ratificada al carácter sobre veredicto técnico.

**Item 9 NUEVO §10.1 CERRADO "no aplica empíricamente" al carácter** + reclasificación deuda Bloque 4 Fase 6 (PositionsOverlay/OrdersOverlay → ISeriesPrimitive migration).

---

## §5 — 5 Edits docs-only PLAN-MAESTRO-POST-S40.md + commit f70df33 + push

### §5.1 Baseline PLAN MAESTRO pre-Edits

```
$ wc -l refactor/PLAN-MAESTRO-POST-S40.md
     234 refactor/PLAN-MAESTRO-POST-S40.md
$ md5 refactor/PLAN-MAESTRO-POST-S40.md
MD5 (refactor/PLAN-MAESTRO-POST-S40.md) = 410fcf9d764dc8b882a2c3a5c8842295
```

### §5.2 5 Edits aplicados bicapa Claude Code + zsh nativo Ramón

| # | Sección | Línea | Cambio resumido | md5 post-Edit | wc -l |
|---|---|---|---|---|---|
| 1 | §1.2 fila item 9 | L47 | `⏳ ABIERTO prioridad 1 s41 \| —` + `L2914` → `✅ CERRADO "no aplica empíricamente" \| s41` + `L2922` | `ccb0e1b1da79c22ea64a98a672d74493` | 234 |
| 2 | §2.1 fila s41 | L87 | `Análogo arquitectónico a 5f.2 — caracterización + Edit + smoke producción 3 paths` + `L2914` → caracterización empírica + reclasificación deuda + `L2922` | `7e2903fc5cdb569d0629e27dc0dd5112` | 234 |
| 3 | §2.4 Bloque 4 Fase 6 | L122 NEW | (inserción nueva tras L121 `lib/trading/`) → bullet PositionsOverlay/OrdersOverlay → ISeriesPrimitive (deuda heredada item 9 + patrón fase 5g) | `b42e96b2c743a0cd0fd8fe97b4bd143a` | 235 |
| 4 | §5 criterio salida | L214 | `3 items abiertos: 3, 5, 9 NUEVO + 1 bloqueado terceros 6` → `2 items abiertos: 3, 5 + 1 bloqueado terceros 6 + item 9 NUEVO cerrado s41 "no aplica empíricamente"` | `d807ab8b972bb1fd53ff62e6267d68d1` | 235 |
| 5 | §6 próxima sesión | L225 (header) | `## §6 — Próxima sesión = sesión 41` + bloque PASO 0-1-2 viejo → `= sesión 42` + bloque s41 ejecutada al carácter + PASO 0-1-2 s42 + contador §14 `~10` → `~11` | `197c0f13dd58daec17d7edd448f95ca3` | 237 |

Cada Edit verificado bicapa REAL (Claude Code + zsh nativo Ramón coinciden bytes-on-disk) al carácter — patrón canónico s23-s40.

### §5.3 Verificación final consolidada PASO 7

```
$ git status --short
 M refactor/PLAN-MAESTRO-POST-S40.md
$ git --no-pager diff --stat
 refactor/PLAN-MAESTRO-POST-S40.md | 19 +++++++++++--------
 1 file changed, 11 insertions(+), 8 deletions(-)
$ grep -c "9 NUEVO" refactor/PLAN-MAESTRO-POST-S40.md
3
$ grep -c "item 9" refactor/PLAN-MAESTRO-POST-S40.md
3
$ wc -l components/_SessionInner.js
    3045 components/_SessionInner.js
$ md5 components/_SessionInner.js
MD5 (components/_SessionInner.js) = 6eaa3b56a8252277e9073245623f8f45
$ grep -c "cr\.series\.setData\|cr\.series\.update" components/_SessionInner.js
0
$ grep -c "computePhantomsNeeded" components/_SessionInner.js
3
```

Aritmética bicapa al carácter: `git diff --stat` +11/-8 = +3 netas coincide con `wc -l` PLAN MAESTRO 234→237. **§52 NUEVA satisfecha al carácter — cero error aritmética s41**.

3 invariantes fase 4 intactas vigesimosegunda sesión consecutiva. `_SessionInner.js` baseline post-s40 INTACTO al carácter.

### §5.4 Commit `f70df33` + push origin/main fast-forward

Commit message multilínea heredoc `/tmp/commit-msg-s41-plan-maestro.txt` (26 líneas, verificado mecánicamente §52 NUEVA — NO predicho):

```
$ wc -l /tmp/commit-msg-s41-plan-maestro.txt
      26 /tmp/commit-msg-s41-plan-maestro.txt
$ head -1 /tmp/commit-msg-s41-plan-maestro.txt
docs(plan-maestro): cerrar item 9 §10.1 no aplica empiricamente + reclasificar deuda Bloque 4 Fase 6 trading domain
```

Commit ejecutado al carácter:

```
$ git add refactor/PLAN-MAESTRO-POST-S40.md
$ git status --short
M  refactor/PLAN-MAESTRO-POST-S40.md
$ git commit -F /tmp/commit-msg-s41-plan-maestro.txt
[main f70df33] docs(plan-maestro): cerrar item 9 §10.1 no aplica empiricamente + reclasificar deuda Bloque 4 Fase 6 trading domain
 1 file changed, 11 insertions(+), 8 deletions(-)
```

Sin trailer Co-Authored-By (verbatim estricto §49). Patrón canónico bicapa coherente con cadena commits s23-s40.

Pre-push verificación bicapa al carácter:

```
$ git status --short
$ git rev-parse --short HEAD
f70df33
$ git log --oneline origin/main..HEAD | cat
f70df33 docs(plan-maestro): cerrar item 9 §10.1 no aplica empiricamente + reclasificar deuda Bloque 4 Fase 6 trading domain
$ git rev-parse --short origin/main
0106ad3
```

Range push fast-forward `0106ad3..f70df33` (1 commit docs-only). Cero contaminación.

Push ejecutado al carácter:

```
$ git push origin main
Enumerating objects: 7, done.
Counting objects: 100% (7/7), done.
Delta compression using up to 10 threads
Compressing objects: 100% (4/4), done.
Writing objects: 100% (4/4), 2.44 KiB | 2.44 MiB/s, done.
Total 4 (delta 3), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (3/3), completed with 3 local objects.
To https://github.com/rammfxtrading-cpu/forex-simulator-algorithmic-suite.git
   0106ad3..f70df33  main -> main
```

`origin/main` post-push = `f70df33`. Runtime Vercel efectivo permanece `ae29f16` (docs-only — bundle build idéntico).

**Item 9 NUEVO §10.1 CERRADO ESTRUCTURALMENTE AL CARÁCTER POST-CARACTERIZACIÓN EMPÍRICA EN REPO**.

---

## §6 — Errores §9.4 propios CTO s41 registrados al carácter sin maquillaje

### §6.1 Error #1 — instrucción ambigua PASO 2 Edit 1 + verificación combinadas

PASO 2 CTO emitió DOS bloques en mismo mensaje:
1. Declaración Edit 1 (old_str / new_str)
2. Comandos verificación bicapa post-Edit (wc -l + md5 + grep)

Sin separar orden ejecución explícito. Ramón interpretó razonablemente como bloque ejecutable secuencial → ejecutó solo verificación. Output bytes-on-disk reveló md5 idéntico baseline `410fcf9d...` = Edit no aplicado.

CTO debió emitir:
- Paso aislado Edit 1 (str_replace) → Claude Code ejecuta + reporta
- Paso siguiente independiente verificación bicapa

Patrón canónico bicapa s23-s40 = un comando ejecutable por paso + verificación post separada.

**Beneficio colateral discriminante**: bytes-on-disk pre-Edit ratificados intactos al carácter. Baseline ratificado. Cero contaminación.

CTO reconoció error sin maquillaje al carácter + corrigió emitiendo PASO 2 reformulado con Edit aislado a Claude Code + verificación separada zsh nativo Ramón.

### §6.2 Error #2 — propuesta heredoc literal pegado en chat para HANDOFF largo

PASO 10a CTO propuso entregar HANDOFF s41 (~400 líneas) vía heredoc literal pegado en mensaje chat web Claude para que Ramón lo pegase a Claude Code y Claude Code creara el archivo via heredoc bash.

Violación patrón canónico bicapa entrega HANDOFFs heredado s23-s40:
- HANDOFFs largos s23/s24/.../s32/.../s40 se entregan como **archivo descargable** vía `~/Downloads/HANDOFF-cierre-sesion-N.md`
- Ramón ejecuta `mv ~/Downloads/HANDOFF-cierre-sesion-N.md refactor/`
- Después `git add` + `git commit -F /tmp/commit-msg-handoff-sN.txt` + `git push`

HANDOFF s32 §7.1 documentaba el patrón verbatim — leído en project_knowledge_search arranque s41. CTO ignoró patrón al redactar HANDOFF s41. Match clase §53 NUEVA pattern olvido — generalizar nuevo método sin verificar patrón canónico documentado bytes-on-disk.

Cazado por Ramón al carácter instancia §14 #4 s41 verbatim:

> "pero k es todos estos mensajes? asi fragmentados, separados.... a ver.. si es el hndoff damelo como archivo , lo descargo y me das los comandos pertinentes"

CTO reconoció error sin maquillaje + procedió patrón canónico (archivo descargable + comandos `mv` + commit + push). **Lección §54 NUEVA origen** §7.

### §6.3 Patrón consistente al carácter

Los 2 errores §9.4 propios CTO en s41 comparten patrón: **generalización contextual sin verificar patrón canónico documentado bytes-on-disk en cada caso**. Error #1 generalizó "emitir Edit + verificación juntos" sin verificar patrón canónico "un comando ejecutable por paso". Error #2 generalizó "heredoc literal" sin verificar patrón canónico "archivo descargable + mv + commit" documentado HANDOFF s32 §7.1.

§9.4 lección consolidada al carácter: cada decisión sobre **método de entrega** debe ratificarse contra patrón canónico bicapa documentado en HANDOFFs previos ANTES de emitir. Disciplina bicapa Ramón cazó 2 de 2 errores al carácter.

---

## §7 — Lecciones consolidadas al carácter

### §7.1 Lecciones aplicadas en s41

- **§14 (intuición Ramón = input técnico encriptado)** vigesimocuarta sesión consecutiva MULTI-INSTANCIA: 4 instancias decisivas s41 catalogadas §9.

- **§15 (NO improvisar fix sin diagnóstico)**: aplicado al carácter sobre item 9 — caracterización empírica L2900-L2940 + typings.d.ts LWC oficial antes de proponer estrategia.

- **§38 (agotar diagnóstico empírico en bytes propios ANTES de buscar externamente)**: aplicado al carácter sobre item 9 — caracterización bytes-on-disk con grep recursivo sobre `setInterval`, `subscribePriceScale`, `IPriceScaleApi`, canales pub/sub LWC oficial typings.d.ts antes de cualquier propuesta de estrategia.

- **§43 (enumerar TODOS los paths antes de declarar Edit cerrado)**: aplicado al carácter sobre PLAN MAESTRO — 5 sitios enumerados (§1.2 L47 + §2.1 L87 + §2.4 L122 + §5 L214 + §6 L225) ANTES de arrancar Edits.

- **§44 (caracterización empírica DOS veces)**: aplicado al carácter sobre cada Edit — verificación bicapa Claude Code + zsh nativo Ramón post cada Edit.

- **§46 (profundizar inventario en bytes ANTES de decidir)**: aplicado al carácter — lectura íntegra PLAN MAESTRO L1-L234 (3 sed) + L2900-L2940 _SessionInner.js + typings.d.ts LWC oficial 5.x antes de cualquier Edit.

- **§47 (entregable tangible cada sesión)**: aplicado al carácter — la sesión termina con 1 commit docs-only (`f70df33`) + push origin/main + 5 Edits PLAN MAESTRO + caracterización empírica documentada + reclasificación deuda Bloque 4 Fase 6 + fe-de-erratas HANDOFF s40 §5 + 2 errores §9.4 propios CTO + 2 lecciones nuevas + HANDOFF s41 (este documento). **Seis entregables tangibles** s41.

- **§48 (LWC oficial precede vendor fork)**: ratificado al carácter sobre item 9 — caracterización canales pub/sub LWC oficial 5.x antes de considerar vendor fork. Resultado: LWC oficial NO expone canal price scale change → estrategia C (cerrar "no aplica empíricamente") + reclasificación deuda Bloque 4 Fase 6 (migración ISeriesPrimitive LWC oficial pattern fase 5g).

- **§49 (HANDOFF requiere ejecución bytes-on-disk REAL)**: aplicada al carácter en ESTE HANDOFF s41 recursivamente. Cada verificación bicapa registrada corresponde a comando REAL ejecutado por Ramón en zsh con output verbatim transcrito desde mensajes pegados en chat. Cero transcripción de memoria.

- **§50 (verificación discriminante debe modelar el runtime real del artifact)**: aplicado al carácter — `_SessionInner.js` baseline post-s40 md5 `6eaa3b56...` INTACTO al cierre + 3 invariantes fase 4 PASS = runtime real artifact código intacto. PLAN MAESTRO md5 transición ratifica runtime artifact docs-only.

- **§51 NUEVA s39 (items diferidos en HANDOFFs sucesivos requieren re-verificación empírica bytes-on-disk)**: aplicada al carácter en arranque s41 sobre item 9 §10.1. Grep amplio `setInterval` retornó 2 matches discriminados al carácter (L2922 candidato VIVO + useAuth.js fix s32 legítimo). Resultado: item 9 VIVO confirmado bytes-on-disk (no fantasma).

- **§52 NUEVA s40 (contar líneas mecánicamente ANTES aritmética)**: aplicada al carácter — NO predije aritmética líneas Edits ni HANDOFF heredoc commit message. Bytes-on-disk wc -l verificación mecánica en cada paso. Cero error aritmética s41 vs 5 errores s40 §6.1.

- **§53 NUEVA s40 (pregunta diagnóstica Ramón ≠ orden cambio plan)**: aplicada al carácter — instancia §14 #4 s41 ("pero k es todos estos mensajes?") fue corrección sistémica método entrega, NO pregunta diagnóstica. CTO interpretó correctamente como orden cambio método ("damelo como archivo, lo descargo y me das los comandos") + reconoció error #2 sin maquillaje + procedió patrón canónico.

### §7.2 Lecciones nuevas al carácter en s41

**Lección operativa s41 — artifact `</p>` renderer Claude Code UI ≠ bytes-on-disk reales**.

Output verbatim Claude Code chat UI puede contener artifacts renderer HTML (`</p>`, otros tags) que NO existen bytes-on-disk reales. Ratificación bicapa propia zsh nativo Ramón discriminante obligatoria sobre output Claude Code chat UI ante cualquier artifact inesperado.

Origen al carácter: Edit 4 §5 criterio salida output Claude Code mostró `</p>` espurio al final línea L214. Verificación discriminante `grep -c "</p>" refactor/PLAN-MAESTRO-POST-S40.md = 0` ratificó bytes-on-disk reales SIN contaminación.

Aplicabilidad al carácter: ante artifact inesperado output Claude Code (HTML tags, caracteres especiales, encoding extraño), ejecutar comando discriminante zsh nativo (`grep -c` sobre patrón sospechoso) antes de declarar bytes-on-disk contaminados o asumir contaminación. **Default = artifact UI hasta probar lo contrario bytes-on-disk**.

**Lección §54 NUEVA — HANDOFFs largos ENTREGABLES como archivo descargable, NUNCA pegados verbatim en chat ni redactados via heredoc Claude Code instruido desde CTO web**.

HANDOFFs largos (>100 líneas) tienen patrón canónico bicapa entrega documentado HANDOFF s32 §7.1:
1. CTO web crea archivo en sandbox propio vía `create_file` (no en repo Ramón)
2. CTO web emite `present_files` para que Ramón descargue archivo
3. Ramón descarga a `~/Downloads/HANDOFF-cierre-sesion-N.md`
4. Ramón ejecuta `mv ~/Downloads/HANDOFF-cierre-sesion-N.md refactor/`
5. Ramón verifica `ls -la refactor/HANDOFF-cierre-sesion-N.md` + `wc -l refactor/HANDOFF-cierre-sesion-N.md`
6. Ramón ejecuta `git add refactor/HANDOFF-cierre-sesion-N.md`
7. Ramón ejecuta commit message multilínea via `/tmp/commit-msg-handoff-sN.txt` + `git commit -F`
8. Ramón ejecuta `git push origin main`

Patrón viola NUNCA:
- Heredoc literal pegado en chat web Claude esperando que Ramón lo pegue a Claude Code (fragmentado, error-prone, no descargable, no archivable localmente)
- Heredoc bash instruido desde CTO web a Claude Code para crear archivo HANDOFF directamente en `refactor/` (salta paso `~/Downloads/` + verificación independiente Ramón sobre archivo COMPLETO antes de añadirlo al repo)

Origen al carácter: error #2 s41 sistémico. CTO web propuso heredoc literal pegado en chat (~400 líneas) violando patrón canónico s32 §7.1. Cazado por Ramón verbatim "damelo como archivo, lo descargo y me das los comandos pertinentes".

Aplicabilidad al carácter: ante necesidad entregar HANDOFF largo (>100 líneas), CTO web SIEMPRE:
1. Crea archivo en sandbox propio via `create_file`
2. Emite `present_files` para descarga
3. Emite comandos `mv` + verificación + commit + push como pasos separados zsh nativos Ramón
4. NUNCA pega heredoc literal en chat ni instruye Claude Code a crear el HANDOFF via heredoc bash

---

## §8 — Fe-de-erratas HANDOFF s40 §5 (L2914 → L2922)

### §8.1 Declaración HANDOFF s40 §5 verbatim

HANDOFF s40 §5 declaró al cierre:

> "Post-Edit s40 polling 150ms zoom Y se renumera a L2914 (consecuencia -13 Edit A)."

### §8.2 Realidad bytes-on-disk post-s40 completo

```
$ grep -n "setInterval(update,150)" components/_SessionInner.js
2922:    const iv=setInterval(update,150)
```

Línea real bytes-on-disk al carácter post-s40 completo = **L2922**, NO L2914.

### §8.3 Causa raíz al carácter

HANDOFF s40 §5 contó solo efecto Edit A (-13 → 2927-13 = 2914) **olvidando sumar efecto Edit B** (+8 líneas insertadas en L443, antes del polling 150ms). Aritmética correcta post-s40:

```
L2927 baseline pre-s40 (polling 150ms posición original)
- 13 (Edit A delete polling 300ms L420-L438)
+ 8  (Edit B extender handler subscribeClick L443 con rama SET)
= L2922 post-s40 completo
```

Match clase **§52 NUEVA — declarar línea exacta sin verificar mecánicamente bytes-on-disk post-completo**.

### §8.4 NO afecta runtime ni invariantes

- Runtime Vercel `ae29f16` intacto al carácter.
- 3 invariantes fase 4 intactas al carácter (`setData|update` = 0, `computePhantomsNeeded` = 3, Cluster A §1.7 intocado).
- Cero impacto código.

### §8.5 Errata documental corregida s41 al carácter

PLAN-MAESTRO-POST-S40.md errata corregida en 3 sitios al carácter:
- §1.2 fila item 9 (L47): `L2914` → `L2922`
- §2.1 fila s41 (L87): `L2914` → `L2922`
- §2.4 Bloque 4 nota (L122): `L2922` verbatim en bullet nuevo

HANDOFF s40 §5 bytes-on-disk NO modificado (documento histórico inmutable post-cierre s40). Errata documentada aquí §8 para trazabilidad histórica al carácter.

---

## §9 — Lección §14 vigesimocuarta sesión consecutiva al carácter MULTI-INSTANCIA

S41 produjo 4 instancias decisivas §14 catalogadas al carácter:

| # | Instancia | Verbatim Ramón | Decodificación técnica | Aplicación CTO |
|---|---|---|---|---|
| 1 | Veredicto cierre item 9 (PASO 1 fin) | "lo que sea lo mejor para el proyecto" | delegación juicio CTO sobre veredicto técnico item 9 + estrategia A/B/C | Estrategia C elegida al carácter (cerrar "no aplica empíricamente") + reclasificación deuda Bloque 4 Fase 6 + 5 Edits docs-only PLAN MAESTRO + commit + push |
| 2 | Plan 10 pasos ratificación (PASO 1 final) | "lo k sea lo mejor" | confianza juicio CTO sobre scope/orden Edits PLAN MAESTRO | Plan 10 pasos arrancado al carácter — PASO 2 Edit 1 §1.2 |
| 3 | Continuar HANDOFF o parar (PASO 10 inicio) | "lo k sea lo mejor" | delegación juicio CTO sobre redactar HANDOFF s41 ahora vs parar sesión con commit `f70df33` pusheado | HANDOFF s41 redactado al carácter inmediatamente — §47 entregable tangible + §49 bytes-on-disk REAL frescos en chat |
| 4 | Corrección método entrega HANDOFF (PASO 10a inicio) | "pero k es todos estos mensajes? asi fragmentados, separados.... si es el hndoff damelo como archivo , lo descargo y me das los comandos pertinentes" | corrección sistémica método entrega HANDOFF — patrón canónico bicapa s32 §7.1 (archivo descargable + mv + commit + push) viola heredoc literal pegado en chat | **CTO reconoció error #2 sin maquillaje** + procedió patrón canónico (archivo descargable via `create_file` + `present_files` + comandos `mv` + commit + push como pasos separados). Lección §54 NUEVA origen |

Patrón consistente al carácter con instancias previas §14 documentadas s31-s40:
- s40 5 instancias multi (s40 §8)
- s39 9 instancias multi (s39 §8)
- s38 instancia 1 "los 4 pas... si hubiera alguna cosa mal te lo digo.."
- s37 instancia 1 "pero y que hemos hecho para que quieras redctar handoff ya?"
- s36 "y lo k ya esta pintado de las killzones no se puede anclar al precio y hora?"
- s35 "k kieres k haga? probar lo k ya sabemos k esta mal?"
- s34 "el ResizeObserver no es el problema, mira tu logs"
- s33 "no le veo sentido al instrumentar X"

Vigesimocuarta sesión consecutiva al carácter §14 input técnico encriptado. S41 4 instancias decisivas con patrón delegación juicio CTO (3 instancias) + corrección sistémica método entrega (1 instancia error #2 sistémico).

---

## §10 — Items diferidos post-s41 + plan sesión 42

### §10.1 Items §10.1 al carácter al cierre s41

| # | Item | Origen | Estado al cierre s41 |
|---|---|---|---|
| 1 | 5f.2 polling cleanup | diferido s28 | ✅ CERRADO s40 `ae29f16` |
| 2 | 5e.4 debugCtx cleanup | diferido s29 | ✅ CERRADO s39 `e44bb9b` |
| 3 | 5d.7-5d.8 viewport preservation | diferido s30 | ⏳ ABIERTO — **prioridad 1 s42** |
| 5 | Debt 5.1 viewport timeframe change | TradingView-style | ⏳ ABIERTO — prioridad 1 s42-s43 (sesión arquitectónica dedicada) |
| 6 | Datos crudos Giancarlo/Luis | diferido s30 | ⏳ ABIERTO — bloqueado terceros |
| 7 | 5f LS-DEBUG cleanup | diferido s23 | ✅ CERRADO FANTASMA s39 §3 |
| 8 | `CustomDrawingsOverlay.js` S33.4 | diferido s35 §6 | ✅ CERRADO "no aplica empíricamente" s39 §4 |
| 9 NUEVO | Polling 150ms zoom Y L2922 `_SessionInner.js` | detectado s40 §5 | ✅ CERRADO s41 "no aplica empíricamente" — reclasificada deuda Bloque 4 Fase 6 |

**Items §10.1 abiertos restantes**: 3 (items 3 + 5 + 6 bloqueado terceros). Patrón Bloque 1 cleanup §10.1 según PLAN MAESTRO §2.1 actualizado s41.

### §10.2 Plan sesión 42 — propuesta CTO

**PASO 0 s42**: baseline verificación bicapa REAL (§49 + §51 NUEVA aplicada):
1. `git status --short` → vacío esperado
2. `git rev-parse --short HEAD` → `<HASH-HANDOFF-s41>` esperado
3. `git log --oneline -5 | cat` → HANDOFF s41 + f70df33 PLAN-MAESTRO s41 + 0106ad3 PLAN-MAESTRO s40 + efa197f HANDOFF s40 + ae29f16 cleanup 5f.2
4. `wc -l components/_SessionInner.js` → 3045 esperado (post s40-5f.2)
5. md5 archivo → `6eaa3b56a8252277e9073245623f8f45` esperado
6. 3 invariantes fase 4 verificación REAL
7. **§51 NUEVA aplicada al item §10.1 prioritario s42**: re-verificar empírica bytes-on-disk ANTES de asumir vivo

**PASO 1 s42**: caracterización empírica items 3+5 viewport preservation TradingView-style. Sesión arquitectónica dedicada §2.1 fila s42-s43:
- Caracterización vs FX Replay/TradingView empírica
- Atajo Opt+R/Alt+R reset viewport
- Comportamiento actual al carácter `chartViewport.js` §1.7 sub-fase 5d.5-5d.6
- Items 3 (5d.7-5d.8) + 5 (Debt 5.1 viewport timeframe change) caracterización conjunta

**PASO 2-N s42**: dependiente decisión PASO 1.

### §10.3 Riesgos identificados al carácter para s42

- **Cluster A `lib/chartViewport.js` §1.7 PROTEGIDO** — items 3+5 cambios viewport pueden tocar Cluster A. Caracterización empírica obligatoria pre-Edit con autorización Ramón explícita ANTES de tocar §1.7 (vigesimosegunda sesión consecutiva intocado al carácter).
- Sesión arquitectónica dedicada exige PASO 0 caracterización empírica vs FX Replay/TradingView en navegador real. Comparación visual + comportamiento + atajos teclado.
- Decisión arquitectónica al carácter Ramón requerida (qué TradingView-style replicar, qué FX Replay-style descartar).

---

## §11 — Cierre sesión 41

Sesión 41 cerrada al carácter 27 mayo 2026, ~11:30 hora local.

HEAD local main = `f70df33` (commit `f70df33` docs(plan-maestro) cerrar item 9) — pendiente añadir commit HANDOFF s41 al cerrar.
`origin/main` = `f70df33` (pusheado).
Producción Vercel runtime efectivo = `ae29f16` (sin cambio — deploy automático docs-only bundle idéntico).

**Item 9 NUEVO §10.1 CERRADO ESTRUCTURALMENTE AL CARÁCTER POST-CARACTERIZACIÓN EMPÍRICA EN REPO**.

1 item §10.1 procesado al carácter en s41:
- Item 9 NUEVO (polling 150ms zoom Y L2922) — ✅ CERRADO "no aplica empíricamente" — caracterización empírica + 5 Edits docs-only PLAN MAESTRO + commit + push

2 archivos modificados/nuevos al carácter:
- `refactor/PLAN-MAESTRO-POST-S40.md` MODIFICADO 234→237 líneas (+3 netas) md5 `197c0f13dd58daec17d7edd448f95ca3`
- `refactor/HANDOFF-cierre-sesion-41.md` NEW (este documento)

1 commit docs-only al carácter:
- `f70df33 docs(plan-maestro): cerrar item 9 §10.1 no aplica empiricamente + reclasificar deuda Bloque 4 Fase 6 trading domain`

CERO archivos código modificados al carácter. `_SessionInner.js` baseline post-s40 INTACTO.

2 errores §9.4 propios CTO registrados al carácter sin maquillaje:
- Error #1: instrucción ambigua PASO 2 Edit + verificación combinadas (cazado bicapa REAL discriminante)
- Error #2: propuesta heredoc literal pegado en chat para HANDOFF largo viola patrón canónico bicapa s32 §7.1 (cazado por Ramón verbatim instancia §14 #4)

1 lección operativa nueva al carácter:
- Artifact `</p>` renderer Claude Code UI ≠ bytes-on-disk reales — verificación discriminante zsh nativo obligatoria

1 lección §54 NUEVA al carácter:
- HANDOFFs largos ENTREGABLES como archivo descargable (vía `create_file` sandbox CTO web + `present_files` → `~/Downloads/` → `mv` → commit + push), NUNCA pegados verbatim en chat ni redactados via heredoc Claude Code instruido desde CTO web

1 fe-de-erratas HANDOFF s40 §5 corregida al carácter:
- L2914 → L2922 (HANDOFF s40 §5 contó solo Edit A olvidando Edit B). Errata documentada §8 para trazabilidad histórica.

Lección §51 NUEVA s39 aplicada al carácter en arranque s41 sobre item 9 (re-verificación empírica bytes-on-disk confirma item VIVO no fantasma).

Lección §14 vigesimocuarta sesión consecutiva al carácter MULTI-INSTANCIA: 4 instancias decisivas s41 catalogadas §9.

3 invariantes fase 4 intactas vigesimosegunda sesión consecutiva al carácter.
Cluster A §1.7 `lib/chartViewport.js` intocado vigesimosegunda sesión consecutiva.

PLAN MAESTRO POST-S40 actualizado al carácter en repo (commit `f70df33`):
- §1.2 item 9 cerrado
- §2.1 fila s41 ejecutada
- §2.4 Bloque 4 nota deuda heredada PositionsOverlay/OrdersOverlay → ISeriesPrimitive
- §5 criterio salida 3→2 items abiertos
- §6 próxima sesión = s42

Próxima sesión = sesión 42. Prioridad 1 sugerida = items 3+5 §10.1 viewport preservation TradingView-style (sesión arquitectónica dedicada). **Aplicar §49 + §51 + §52 + §53 + §54 NUEVA al carácter en HANDOFF s42**: cada verificación bicapa ejecutada REALMENTE en zsh + output verbatim transcrito. NO transcribir de memoria. Conteo aritmético mecánico. Pregunta Ramón post-ejecución = pregunta diagnóstica default. Item diferido re-verificación empírica bytes-on-disk obligatoria. HANDOFF largo entrega como archivo descargable patrón canónico.

**Cleanup deuda técnica tercer item cerrado estructural post-fase-5g al carácter** (s39 debugCtx + s40 polling 300ms + s41 polling 150ms "no aplica"). Disciplina bicapa estricta + §49 + §51 + §52 + §53 + §54 NUEVA reforzados/nacidos. Calidad TradingView no negociable. CLAUDE.md §1.

— CTO
