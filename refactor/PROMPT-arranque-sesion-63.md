# PROMPT ARRANQUE SESIÓN 63 — forex-simulator-algorithmic-suite

## 0. Contexto fijo (no cambia)
- CTO/revisor = chat web (razona y verifica en sandbox propio); Ramón Tarinas (trader, NO dev) ejecuta en zsh de su iMac. Bytes en disco = única verdad. Español, tono CTO, UN paso por mensaje corto.
- Repo: /Users/principal/Desktop/forex-simulator-algorithmic-suite · Prod: simulator.algorithmicsuite.com (Vercel, deploy on push a main) · BD Supabase epxoxxadclhfnwfuwoyx.
- macOS: `md5` / `md5 -q` (no md5sum). Git siempre con `| cat`. Heredoc SIEMPRE quoted ('EOF').
- Gate §3.1: ejecutar `git push` ES el OK nominal de Ramón; smoke de producción tras cada push.
- Disciplina de parche: python con assert md5 base + unicidad de anclas; ABORT sin escribir si algo difiere. Guard de transporte: `test "$(md5 -q /tmp/parcheX.py)" = "<md5>" && python3 ... || echo NO_EJECUTADO`.
- REGLAS vigentes (s59-s62): (1) heredoc SIEMPRE transcrito del view del archivo del sandbox; (2) transcripción AL CARÁCTER incluidas secuencias de escape; (3) ningún byte de un assert ni md5 de transporte nace del ojo del CTO — sandbox-first sin excepciones; (4) smokes en cuarto limpio (reiniciar dev + recarga dura; tras cambiar node_modules además `rm -rf .next`); (5) tests destructivos, también los NEGATIVOS, solo sobre cuentas/sesiones sacrificables; (6) cada paste de Ramón abre terminal nueva en ~: TODO bloque lleva el cd embebido; (7) otro agente (Claude Code) solo read-only, su informe se contrasta contra bytes; (8) NUEVA: grep de constantes numéricas SIEMPRE tolerante a espacios — el *10 de pricing se escondía como espacio-10 y un grep literal lo perdió.

## 1. Estado al cierre de s62 (noche 6-jun-2026)
- **s62 cerró 4 frentes (3 pushes + recon):** PASO 0 bicapa PASS · poda de muertos en dashboard.js 620→578 (prod `b727488`: fuera import EquityCurve —el componente VIVE en admin.js L6/L645—, selectedSession con setter jamás invocado, memo reducido a closedTrades/wins/totalPnl con deps [trades]) · warning dev borderColor/border muerto (prod `6360978`: pillPause y togOn de sessionStyles.js a shorthand completo; ReplayPill era 1:1 de estilos importados, cero hits) · duplicate key borderRadius de emptyCard muerto (prod `7e2664b`). Runtime producción = `7e2664b`; encima va el commit docs de cierre — PASO 0 debe verificar ese delta como solo-docs.
- Archivos clave: _SessionInner.js 1499/`c6c4d714` · dashboard.js **578**/`3e23d78b` · sessionStyles.js **104**/`dd5b0830` · ReplayPill.js 96/`6ddd3de2` · pricing.js 15/`a8cee369` · admin.js 1134/`925c6d7f` · AntimatterLoader.js 376/`f64335ba` · wipe-simulador.js 112/`dc59f4de` · PLAN-MAESTRO con md5 nuevo impreso por el parche4 de cierre.
- **BD baseline cierre s62:** trades **156** (los 2 nuevos = Ramón operando la sesión-repro en el smoke prod, P&L +1049.81; pueden venir tagueados por la feature de tagging → tagueados esperado 0-2, verificar en PASO 0) · sesiones 25 · drawings 22 · plantillas 14 · chart_config 3 · sin tablas backup. Condicional sesión-repro: si Ramón la borra antes de s63, esperar 24/21 y −2 trades.
- Smokes s62: dev cuarto limpio + prod tras cada push, 3/3 PASS. Un `Object is disposed` aislado en teardown del chart (solo-dev, correlacionado con Fast Refresh, no reproducido en segundo ciclo) anotado como observación.
- Errores CTO s62: **2** de formulación, ambos cazados en bytes antes de disco (ubiqué el warning en ReplayPill.js cuando la raíz era sessionStyles.js; ubiqué el duplicate key en sessionStyles cuando era el objeto s del dashboard). Cero a disco, cero NO_EJECUTADO; predicción de líneas exacta en los 3 parches. Racha: 3→2→4→4→2→2.

## 2. Agenda s63 (orden propuesto)
1. **PASO 0** bicapa: repo (wc/md5 de §1; HEAD = docs encima de `7e2664b`, delta solo refactor/) + BD vs baseline (156 trades, tagueados 0-2, condicional sesión-repro).
2. **Mini-fase pip-value (BLOQUEANTE de apertura, fijado s62 — primera presa):** calcPnl usa 10 $/pip/lote fijo (pips*lots*10), exacto solo con divisa cotizada USD; 7 de 11 pares de ALL_PAIRS mal (USD/JPY, EUR/JPY, GBP/JPY inflan ~40-50%; USD/CHF, USD/CAD, AUD/CAD, EUR/GBP en divisa equivocada). El 10 vive en 8 sitios de 5 archivos: pricing.js L14 (fuente) · orders.js L18 (denominador del RR, acoplado — cambiar calcPnl sin él rompe el RR hoy correcto) · breach.js L49+L54 (motor drawdown del challenge) · PositionOverlay.js L38-39 · LongShortModal.js L120-121 (lotes por riesgo). Fix: pipValue(pair) fuente única en pricing.js enhebrada en los 5, cambio atómico + harness. D-questions Ramón: crosses necesitan cotización de segundo par al instante (EUR/JPY, GBP/JPY → USDJPY; EUR/GBP → GBPUSD; AUD/CAD → USDCAD) que la sesión no carga — decidir aproximación (constante razonable / cotización del propio dataset si existe / retirar crosses hasta tener feed). El plan de la mini-fase nace de un PASO 0 propio en bytes.
3. **Deudas si hay hueco:** asimetría lastBreachIdx · ECONNRESET/401 de /api/candles.
4. Tras "todo bien" de Luis + Giancarlo post-F7 → card PDFs/videos → fase planes/límites (Básico 21 €/6 huecos · Extra 36 €/12 · gate server-side de creación de sesiones + landing de pago en subdominio) → **apertura**.

## 3. Cortes futuros anotados (no urgentes)
- E2 chart-mount: extraer mountPairRef + efecto TF.
- s61-A finde (fix gap-aware aparcado en fork e9a6328, camino correcto = rellenar finde en la serie) · s61-B feed salta vie ~20:45→dom 22:00 · anclar los 6 plugins line-tools a SHA (supply-chain).
