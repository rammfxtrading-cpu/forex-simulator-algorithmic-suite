# PROMPT ARRANQUE SESIÓN 64 — forex-simulator-algorithmic-suite

## 0. Contexto fijo (no cambia)
- CTO/revisor = chat web (razona y verifica en sandbox propio); Ramón Tarinas (trader, NO dev) ejecuta en zsh de su iMac. Bytes en disco = única verdad. Español, tono CTO, UN paso por mensaje corto.
- Repo: /Users/principal/Desktop/forex-simulator-algorithmic-suite · Prod: simulator.algorithmicsuite.com (Vercel, deploy on push a main) · BD Supabase epxoxxadclhfnwfuwoyx.
- macOS: `md5` / `md5 -q` (no md5sum). Git siempre con `| cat`. Heredoc SIEMPRE quoted ('EOF').
- Gate §3.1: ejecutar `git push` ES el OK nominal de Ramón; smoke de producción tras cada push.
- Disciplina de parche: python con assert md5 base + unicidad de anclas; ABORT sin escribir si algo difiere. Guard de transporte: test md5 del /tmp/parcheX.py antes de ejecutar.
- REGLAS vigentes (s59-s63): (1) heredoc SIEMPRE transcrito del view del archivo del sandbox; (2) transcripción AL CARÁCTER incluidas secuencias de escape; (3) ningún byte de un assert ni md5 de transporte nace del ojo del CTO — sandbox-first sin excepciones; (4) smokes en cuarto limpio (reiniciar dev + recarga dura; tras cambiar node_modules además rm -rf .next); (5) tests destructivos, también los NEGATIVOS, solo sobre cuentas/sesiones sacrificables; (6) cada paste abre terminal nueva en ~: TODO bloque lleva el cd embebido; (7) otro agente (Claude Code) solo read-only, su informe se contrasta contra bytes; (8) grep de constantes numéricas SIEMPRE tolerante a espacios; (9) NUEVA: grep de identificadores tolerante a sintaxis JS moderna — `pluginRef.current\.` perdió todos los hits porque el código usa `?.` (optional chaining); patrón `\??\.` siempre.

## 1. Estado al cierre de s63 (mediodía 7-jun-2026)
- **s63 mató el BLOQUEANTE de apertura + una presa derivada, 3 pushes:** PASO 0 bicapa PASS · `7a15441` mini-fase pip-value COMPLETADA: pipValue(pair) fuente única en pricing.js, Opción A tabla por divisa cotizada tras normalizar barra (USD 10 · JPY 6.25 · CHF 12.65 · CAD 7.20 · GBP 13.35, cierres 5-jun-2026; desconocido → 10), 11 sitios en 6 archivos (los 8 mapeados en s62 + 4 del vendor long-short dist que cazó el barrido de la regla 8), RR invariante (numerador y denominador juntos), breach lineal intacto; harness 0 fails + build SIN_HITS + smokes dev/prod PASS; D-question crosses resuelta con tabla estática (exacto-con-feed = refinamiento futuro) · `8545445` bug preexistente hallado en el smoke: dibujo long-short pintaba pips x100 en pares JPY (options.pair nunca fluía al plugin); fix pairRef anti-stale-closure en useDrawingTools + firma buildOptions(toolKey, cfg, pair) + 2 call sites; dibujos viejos sin pair en session_drawings siguen x100 hasta editarse con el modal (auto-sanan) o borrarse, asumido pre-lanzamiento · cierre docs (este commit). Runtime prod = commit docs sobre `8545445`; PASO 0 s64 debe verificar ese delta como solo-docs.
- Archivos clave: pricing.js 20/`5f734440` · orders.js 30/`b8d0e4ca` · breach.js 77/`be788d84` · PositionOverlay.js 186/`88d231a5` · LongShortModal.js 362/`34bb8ac2` · vendor long-short dist 1100/`95b7f2f6` · useDrawingTools.js 246/`9c4cd347` · PLAN-MAESTRO 272/`e184a3ee` · _SessionInner.js 1499/`c6c4d714` · dashboard.js 578/`3e23d78b` · sessionStyles.js 104/`dd5b0830` · ReplayPill.js 96/`6ddd3de2` · admin.js 1134/`925c6d7f` · AntimatterLoader.js 376/`f64335ba` · wipe-simulador.js 112/`dc59f4de`.
- **BD baseline cierre s63:** trades **162** (+6 de los 4 smokes) · sesiones **25** · drawings **23** (+1, long-short del smoke persistido) · plantillas **14** · chart_config **3** · sin tablas backup. Tagueados: 0 en el PASO 0 de s63; los smokes posteriores pueden haber sumado — verificar sin alarma en PASO 0 s64.
- Errores CTO s63: **3** — 2 de formulación (grep `pluginRef.current\.` intolerante a `?.`, salida vacía, raíz de la regla 9; assert de bit-identidad del harness contra el literal 1000, cazado por el propio harness en sandbox) + 1 de transcripción (parche9 intento 1: un dígito de la racha perdido al transcribir; el guard md5 lo paró → NO_EJECUTADO, regenerado en sandbox). Cero a disco. Racha: 3→2→4→4→2→2→3.

## 2. Agenda s64 (orden propuesto)
1. **PASO 0** bicapa: repo (wc/md5 de §1; HEAD = docs encima de `8545445`, delta solo refactor/) + BD vs baseline (162/25/23/14/3, tagueados informativo).
2. **Deudas dev si hay hueco:** asimetría lastBreachIdx · ECONNRESET/401 de /api/candles.
3. Camino apertura: "todo bien" de Luis + Giancarlo post-F7 → card PDFs/videos → fase planes/límites (Básico 21 €/6 huecos · Extra 36 €/12 · gate server-side de creación de sesiones + landing de pago en subdominio) → **apertura** (criterio pip-value ya ✅, criterios en 10).

## 3. Cortes futuros anotados (no urgentes)
- E2 chart-mount: extraer mountPairRef + efecto TF.
- s61-A finde (fix gap-aware aparcado en fork e9a6328, camino correcto = rellenar finde en la serie) · s61-B feed salta vie ~20:45→dom 22:00 · anclar los 6 plugins line-tools a SHA (supply-chain) · pip-value exacto con feed (refinamiento de la Opción A) · dibujos long-short viejos en pares JPY siguen x100 en session_drawings (editar con modal o borrar).
- Observación s62 vigente: `Object is disposed` aislado en teardown del chart, solo-dev, no reproducido.
