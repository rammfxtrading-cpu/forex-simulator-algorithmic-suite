# PROMPT ARRANQUE SESIÓN 64 — forex-simulator-algorithmic-suite

## 0. Contexto fijo (no cambia)
- CTO/revisor = chat web (razona y verifica en sandbox propio); Ramón Tarinas (trader, NO dev) ejecuta en zsh de su iMac. Bytes en disco = única verdad. Español, tono CTO, UN paso por mensaje corto.
- Repo: /Users/principal/Desktop/forex-simulator-algorithmic-suite · Prod: simulator.algorithmicsuite.com (Vercel, deploy on push a main) · BD Supabase epxoxxadclhfnwfuwoyx.
- macOS: `md5` / `md5 -q` (no md5sum). Git siempre con `| cat`. Heredoc SIEMPRE quoted ('EOF').
- Gate §3.1: ejecutar `git push` ES el OK nominal de Ramón; smoke de producción tras cada push.
- Disciplina de parche: python con assert md5 base + unicidad de anclas; ABORT sin escribir si algo difiere. Guard de transporte: test md5 del /tmp/parcheX.py antes de ejecutar.
- REGLAS vigentes (s59-s63): (1) heredoc SIEMPRE transcrito del view del archivo del sandbox; (2) transcripción AL CARÁCTER incluidas secuencias de escape; (3) ningún byte de un assert ni md5 de transporte nace del ojo del CTO — sandbox-first sin excepciones; (4) smokes en cuarto limpio (reiniciar dev + recarga dura; tras cambiar node_modules además rm -rf .next); (5) tests destructivos, también los NEGATIVOS, solo sobre cuentas/sesiones sacrificables; (6) cada paste abre terminal nueva en ~: TODO bloque lleva el cd embebido; (7) otro agente (Claude Code) solo read-only, su informe se contrasta contra bytes; (8) grep de constantes numéricas SIEMPRE tolerante a espacios; (9) grep de identificadores tolerante a sintaxis JS moderna (patrón `\??\.`); (10) NUEVA s63-bis: anclas de parche SIEMPRE en subcadenas ASCII puras — un guion visualmente idéntico con bytes distintos tumbó un ancla; ante texto con unicode, localizar la línea por subcadena ASCII y reescribirla entera. Corolario (parche13): TODO contenido nuevo con unicode viaja DENTRO del payload base64+zlib; las líneas de código del parche son ASCII puro de punta a punta.

## 1. Estado al cierre de s63 (madrugada 8-jun-2026; la sesión tuvo dos tramos)
- **Tramo 1 (mediodía): BLOQUEANTE pip-value muerto + presa derivada, 3 pushes:** PASO 0 bicapa PASS · `7a15441` mini-fase pip-value COMPLETADA: pipValue(pair) fuente única en pricing.js, Opción A tabla por divisa cotizada tras normalizar barra (USD 10 · JPY 6.25 · CHF 12.65 · CAD 7.20 · GBP 13.35, cierres 5-jun-2026; desconocido → 10), 11 sitios en 6 archivos, RR invariante, breach lineal intacto; harness 0 fails + build SIN_HITS + smokes dev/prod PASS · `8545445` fix dibujo long-short pips x100 en pares JPY (pairRef anti-stale-closure en useDrawingTools; dibujos viejos sin pair siguen x100 hasta editarse o borrarse, asumido) · `f2a00bd` cierre docs tramo 1.
- **Tramo 2 (madrugada): feature /operativas EN PRODUCCIÓN, 1 push:** `74798ed` crea pages/operativas.js (483/`95ac7f27`) — página HUÉRFANA (nada la enlaza) con gate useAuth('simulador_activo')+NoAccess desde día uno. Contenido: los 3 trading plans de Ramón (Londres 08:30–11:30 LDN · NY 14:30–17:30 LDN · Swing 4H/30m) renacidos como HTML nativo (los PDFs son maqueta de referencia y JAMÁS se sirven), 3 cards → modales liquid glass con hueco de vídeo + 2 huecos de capturas por modal, y hoja de ruta interactiva de SU metodología: 6 nodos / 11 checks (elegir modelo → estudiar → opción 1/2/3 cada una con sesión propia nombrada «LND · Opción 1» y +100 trades → análisis +300 trades, punto fuerte/débil por opción), botón reset que al 100% se enciende en verde. Persistencia de checks en localStorage clave rammfx_ruta_<user.id> (Supabase = fase blindaje). Red cósmica patrón analytics; scroll propio porque globals.css pone overflow:hidden en body. Card del dashboard L331-338 INTACTA (dashboard sigue 578/`3e23d78b`) = el interruptor final es 1 línea. Parches: 10 (creación, payload base64+zlib, base intermedia 3416926a) · 11 (la regla genérica `.opv-step span` pisaba por especificidad el chip numérico `.opv-n`; pasa a `.opv-step div span`) · 12 (logo real /logo-rammfx.png en vez de la R placeholder). Maqueta aprobada congelada en sandbox CTO (md5 2248ef9a). Smokes: dev 7/7 PASS · prod PASS (con acceso ve todo; incógnito rebota a login; dashboard sin cambiar ni un píxel).
- Archivos clave: pricing.js 20/`5f734440` · orders.js 30/`b8d0e4ca` · breach.js 77/`be788d84` · PositionOverlay.js 186/`88d231a5` · LongShortModal.js 362/`34bb8ac2` · vendor long-short dist 1100/`95b7f2f6` · useDrawingTools.js 246/`9c4cd347` · **pages/operativas.js 483/`95ac7f27`** · _SessionInner.js 1499/`c6c4d714` · dashboard.js 578/`3e23d78b` · sessionStyles.js 104/`dd5b0830` · ReplayPill.js 96/`6ddd3de2` · admin.js 1134/`925c6d7f` · AntimatterLoader.js 376/`f64335ba` · wipe-simulador.js 112/`dc59f4de` · PLAN-MAESTRO 272 líneas (md5 nuevo en el output del parche13, registrar en PASO 0) · PROMPT-s64 (este archivo, reescrito por parche13).
- **BD baseline cierre s63:** trades **162** · sesiones **25** · drawings **23** · plantillas **14** · chart_config **3** · sin tablas backup. OJO s64: Luis + Giancarlo están haciendo backtesting de prueba — un delta de trades/sesiones/drawings en el PASO 0 es ESPERADO, no alarma; verificar coherencia y re-baselinear.
- Errores CTO s63: **7** — tramo 1: 3 (2 formulación + 1 transcripción parada por guard). Tramo 2: #4 ensayo negativo con sed no-op (la línea no contenía el carácter a mutar; cazado en sandbox, re-ensayado con mutación verificada) · #5 el ÚNICO que llegó a disco de Ramón: regla CSS genérica pisaba el chip numérico por especificidad; entró con parche10, lo cazó Ramón en smoke dev ANTES de commit, raíz mal diagnosticada dos veces (culpé al panel de preview) hasta leer la cascada; corregido en parche11; origen de la regla 10 junto al incidente del guion en las anclas · #6 recon con path de PLAN-MAESTRO en raíz sin verificar (vive en refactor/), viaje en balde read-only. · #7 transcripción del parche13 v1: dos líneas nuevas con unicode viajaban como código fuente del parche y el canal las corrompió; guard de transporte → NO_EJECUTADO, regenerado con todo el unicode dentro del payload (corolario regla 10). Cero a commit, cero a producción. Racha: 3→2→4→4→2→2→3→7.

## 2. Agenda s64 (orden propuesto)
1. **PASO 0 bicapa:** repo (HEAD = docs s63-bis encima de `74798ed` encima de `f2a00bd`; wc/md5 de §1 incluido operativas.js; registrar md5 nuevo del PLAN-MAESTRO) + BD vs 162/25/23/14/3 con delta de backtesting ESPERADO.
2. **Contenido /operativas cuando Ramón lo entregue:** vídeos (decidir hosting firmado en fase blindaje; por ahora hueco) + capturas anotadas a los huecos `opv-ex` (parches sobre `95ac7f27`).
3. **Camino apertura actualizado:** "todo bien" de Luis + Giancarlo → **borrado de sus cuentas** (DESTRUCTIVO: con red, verificación en bytes antes/después, regla 5) → **enlazar la card** del dashboard a /operativas (1 línea, L331-338) → **fase planes/límites** (Básico 21 €/6 sesiones · Extra 36 €/12 · campo `plan` con toggle Basic|Extra en la lista "con acceso" de admin, INDEPENDIENTE del acceso y conservado al revocar · límite derivado del plan · **gate server-side en creación de sesiones ANTES del primer alumno de pago** · landing de pago en subdominio) → **apertura** (criterios en 10; pip-value ✅, card avanzada).
4. Deudas dev si hay hueco: asimetría lastBreachIdx · ECONNRESET/401 de /api/candles.

## 3. Cortes futuros anotados (no urgentes)
- E2 chart-mount: extraer mountPairRef + efecto TF.
- s61-A finde (fix gap-aware aparcado en fork e9a6328) · s61-B feed salta vie ~20:45→dom 22:00 · anclar los 6 plugins line-tools a SHA (supply-chain) · pip-value exacto con feed · dibujos long-short viejos en pares JPY siguen x100 en session_drawings.
- /operativas fase blindaje: persistencia de la hoja de ruta por alumno en Supabase · vídeo con streaming firmado y embed bloqueado a dominio · watermark con email del alumno.
- Convención de nombres de sesión («LND · Opción 1») → agrupar métricas por opción en dashboard (futuro).
- Observación s62 vigente: `Object is disposed` aislado en teardown del chart, solo-dev, no reproducido.
