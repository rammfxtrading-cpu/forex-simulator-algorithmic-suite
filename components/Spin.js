/**
 * components/Spin.js — spinner de carga de la página de sesión (Fase 7, Corte B, s59).
 * Extraído 1:1 de components/_SessionInner.js. Depende de la clase .sp del css global
 * (sessionStyles) que SessionPage inyecta via <style>.
 */

export default function Spin(){ return <div className="sp"/> }
